# ── Server config ──────────────────────────────────
PORT=4000
JWT_SECRET=change-me-to-a-real-secret
JWT_EXPIRES_IN=7d

# Comma-separated list of allowed origins (the frontend URL).
# For Vercel: https://your-app.vercel.app
CLIENT_ORIGIN=http://localhost:5173

# ── Supabase (production database) ─────────────────
# Leave both blank to use the local JSON-file store.
# Get these from: Supabase Dashboard → Settings → API
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_URL=https://neuwvyircpdlfqimhrkg.supabase.co
# SUPABASE_SERVICE_KEY=sb_secret_vy70Z9aKB-T-fCeR1mNRsg__Z8XsuCg
